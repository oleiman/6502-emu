# 6502-emu

A simple (enough) 6502 emulator with an extremely dumb debugging interface.

## Build it

```[bash]
$ cd /path/to/6502-emu
$ ./build.sh
```

## Run the tests

```[bash]
$ cd build
$ ./test/catch
```
